{
  "name": "durusuna-backend",
  "version": "1.0.0",
  "description": "Backend API for Durusuna school communication app. Set DB_RESET_MODE=true to reset database on startup.",
  "main": "src/server.ts",
  "scripts": {
    "start": "if [ \"$DB_RESET_MODE\" = \"true\" ]; then echo '🔥 DB_RESET_MODE detected - Running database reset...'; bun run db:force-reset; echo '✅ Database reset completed - Starting server...'; fi && bun src/server.ts",
    "start:seed": "bun scripts/migrate.ts seed && echo 'Seeding completed, container will exit'",
    "dev": "bun --watch src/server.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "test:ci": "bun test --ci --coverage",
    "test:debug": "bun --inspect-brk --test src/",
    "test:service": "bun test tests/userService.test.ts",
    "test:services": "bun test tests/*Service.test.ts",
    "test:legacy": "bun test tests/*.test.js",
    "test:new": "bun test tests/*.test.ts",
    "lint": "eslint src/",
    "db:migrate": "bun scripts/migrate.ts migrate",
    "db:seed": "bun scripts/migrate.ts seed",
    "db:setup": "bun scripts/setup.ts",
    "db:rollback": "bun knex migrate:rollback --knexfile src/knexfile.ts",
    "db:rollback:all": "bun knex migrate:rollback --all --knexfile src/knexfile.ts",
    "db:reset": "bun run db:rollback:all && bun run db:migrate && bun run db:seed",
    "db:force-reset": "bun scripts/reset.ts && bun run db:migrate && bun run db:seed",
    "start:reset": "echo '🔥 Starting with database reset...' && bun run db:force-reset && echo '✅ Database reset completed - Starting server...' && bun src/server.ts",
    "docker:db:reset": "docker compose run --rm backend bun knex migrate:rollback --all --knexfile src/knexfile.ts && docker compose run --rm backend bun knex migrate:latest --knexfile src/knexfile.ts && docker compose run --rm backend bun knex seed:run --knexfile src/knexfile.ts",
    "docker:seed": "docker compose run --rm backend bun knex seed:run --knexfile src/knexfile.ts",
    "docker:migrate": "docker compose run --rm backend bun knex migrate:latest --knexfile src/knexfile.ts",
    "docker:dev": "docker compose up postgres redis -d && bun run docker:db:reset && docker compose up backend -d",
    "test:sevalla": "bun scripts/test-sevalla-connection.ts",
    "debug:railway": "bun scripts/debug-railway-sevalla.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.450.0",
    "@aws-sdk/s3-request-presigner": "^3.450.0",
    "@railway/cli": "^4.5.5",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^1.40.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.0.1",
    "firebase-admin": "^13.4.0",
    "helmet": "^7.0.0",
    "joi": "^17.9.2",
    "jsonwebtoken": "^9.0.2",
    "knex": "^2.5.1",
    "mime-types": "^2.1.35",
    "moment": "^2.29.4",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "multer-s3": "^3.0.1",
    "nodemailer": "^6.9.4",
    "pg": "^8.11.1",
    "sharp": "^0.32.4",
    "socket.io": "^4.7.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^9.0.0",
    "winston": "^3.10.0",
    "zod": "^4.0.10"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/compression": "^1.8.1",
    "@types/express": "^5.0.3",
    "@types/helmet": "^4.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/mime-types": "^3.0.1",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.1.0",
    "eslint": "^8.46.0",
    "jest": "^29.6.2",
    "supertest": "^6.3.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "school",
    "education",
    "chat",
    "communication",
    "api",
    "nodejs",
    "express",
    "socketio"
  ],
  "author": "Durusuna Team",
  "license": "MIT"
}
